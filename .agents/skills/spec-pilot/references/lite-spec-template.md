# 轻量级变更规格（spec.md）模板

> 使用说明：轻量级路径（单点修改）的合并产物——需求、验收标准与实现要点写进一份文档，替代完整级的 SRS + 设计。产物放 `docs/changes/<变更名>/`。已有 `docs/00-constitution.md` 则遵守，没有不新建。"裁剪的是文档，不是验证"：测试用例与测试报告（沿用 04/06 模板）不可省略。

## 1. 变更描述

- **一句话**：（要改什么，如"修复：列表页输入特殊字符会崩溃"）
- **问题来源**：（缺陷 / 用户反馈 / 优化建议，含复现或出处）

## 2. 影响范围

| 项 | 内容 |
|----|------|
| 涉及模块/文件 | （应在一个模块内，否则应升级为完整级） |
| 是否新增接口/表结构/公共契约 | 是 → 重新评估级别；否 → 继续 |
| 连带影响 | （其他调用方、已有数据兼容性；无则写"无"） |

## 3. 验收标准（可自动验证）

| 编号 | 验收标准 | 验证方式 |
|------|----------|----------|
| A-1 | （如"输入含 `&<>"'` 的内容，列表页正常渲染，退出码 0"） | |
| A-2 | | |

要求与 SRS 相同：可执行的检查步骤、可观察的输出、具体数值；"不再崩溃"不算标准。

## 4. 实现要点

（打算怎么改的 3~5 条要点；不画架构图、不写接口定义——改动若复杂到需要它们，就不是轻量级了。）

## 5. 明确不做

（本次变更不碰的部分，防止顺手加戏。）

## 6. 产出物自检

- [ ] 影响范围在一个模块内、无新增接口/表结构（否则升级完整级）
- [ ] 每条验收标准可自动验证
- [ ] 明确不做清单已写（哪怕只有一条）
- [ ] `04-test-cases.md` 已按验收标准生成并冻结
- [ ] 覆盖核对无缺口：第 3 节每条验收标准（A-x）都能在冻结用例的覆盖矩阵中找到至少一条对应用例——轻量级可以裁剪文档，不能裁剪这道核对
