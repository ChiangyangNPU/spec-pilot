# 复盘记录（lessons.md）

> 每次流水线运行结束后追加几行（借鉴 BMAD 的 learn & adjust）。阶段 0 启动时先读本文件。

## 运行 1：todo-cli 示例（2026-09-13）

- **卡壳环节**：无——一轮全绿，未触发缺陷修复循环。
- **度量**：冻结用例 30、首轮全绿（是）、缺陷 0、修复轮次 0/5、单测 42。
- **模板不顺处**：06-test-report-template 的"代码行覆盖率"栏在零依赖约束下无法填写，测试报告以"用例矩阵覆盖"替代并注明原因——模板可考虑给"无法测量时的替代口径"留一行说明。
- **估算偏差**：无工时估算环节，不适用。
- **下次怎么改**：SRS 的 NFR 验证方式若涉及特定平台（NFR-2），建议在验收标准里直接写明"验证环境清单"，避免报告里再补"其他平台未验证"的说明。

## 运行 2：fix-newline-content 轻量级变更（2026-09-13）

- **卡壳环节**：无——轻量级路径单会话完成，4 个变更用例 + 42 个回归用例一轮全绿。
- **度量**：变更用例 4、回归 47 全绿（首轮全绿：是）、缺陷 0（原始缺陷 1 个随变更关闭）、修复轮次 0/5。

## 运行 3：v1.0 对抗式设计评审（2026-09-19）

- **触发**：新评审机制首次真实验证——干净上下文子代理，输入仅 SRS + 设计，任务为对抗式挑毛病。
- **结果**：12 项发现（11 采纳修订、1 项说明理由不采纳），其中最重的一条：**fix-newline-content 合入主 SRS 后沿链更新漏掉了设计文档**——"变更沿文档链向后传播"的规则在示例自己身上断了一处，被评审抓到。
- **教训**：变更传播的完成判据是"每一份下游文档都已更新"，不是"主文档更新了"；对抗式评审对这类执行疏漏的捕捉是有效的，机制值得保留。
- **模板不顺处**：运行 1 提出的"覆盖率替代口径""Lint 承接行""测试与用例 ID 追溯"三项，已在模板修订中固化（对应仓库提交），本次运行直接受益——新模板的 spec.md 让轻量级变更第一次有了固定格式。
- **下次怎么改**：历史数据兼容（A-4 类场景）在 spec 模板的"影响范围-连带影响"里值得单列一问，本次是自行想到的。
