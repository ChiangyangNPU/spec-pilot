# 变更测试报告 —— fix-newline-content

> 阶段 4 产物，模板：06-test-report-template.md。测试唯一依据：本目录冻结的 04-test-cases.md。

## 1. 执行概要

- **轮次**：第 1 轮
- **触发原因**：首次执行（本变更的编码完成后）
- **用例总数**：4 ｜ 通过：4 ｜ 失败：0
- **通过率口径**：通过率 = 通过 / (通过 + 失败) = 4/4 = 100%；本轮无 BLOCKED 用例
- **结论**：全部通过，变更可交付

## 2. 单元测试结果

| 指标 | 结果 |
|------|------|
| 测试命令 | `python3 -m unittest discover -s tests`（退出码 0） |
| 构建 / Lint / 静态检查 | 构建 `python3 -m compileall -q todo tests` 退出码 0；Lint 未执行：环境无 linter 且零依赖约束不引入（沿用主运行 lessons 结论） |
| 用例总数 / 通过 / 失败 | 47 / 47 / 0（0.76s） |
| 代码行覆盖率 | 未测量：零依赖约束不引入 coverage 工具，以用例矩阵覆盖代替 |

新增 5 个测试方法均携带来源用例 ID（追溯约定）：`test_add_newline_rejected` / `test_add_carriage_return_rejected`（单元层 TC-C1/C2）+ `test_add_multiline_rejected` / `test_add_cr_rejected` / `test_legacy_multiline_file_still_loads`（端到端 TC-C1/C2/C4）。

## 3. 功能用例执行结果

| 用例 ID | 来源验收标准 | 结果 | 实际结果 |
|---------|--------------|------|----------|
| TC-C1 | A-1 | ✅ | 退出码 2，stderr 含"换行"，数据文件未写入（子进程复验 + 单测双确认） |
| TC-C2 | A-2 | ✅ | 退出码 2，stderr 含"换行" |
| TC-C3 | A-3 | ✅ | 既有 42 个测试全部通过（全量回归，无一破坏） |
| TC-C4 | A-4 | ✅ | 手工构造多行历史数据文件，`list` 退出码 0、内容正常输出（test_legacy_multiline_file_still_loads） |

## 4. 缺陷列表

| 缺陷 ID | 来源用例 | 复现步骤 | 期望 | 实际 | 根因/修复策略 | 严重程度 | 状态 |
|---------|----------|----------|------|------|---------------|----------|------|
| （本轮无新增缺陷；原始缺陷即本变更主题） | | | | | 原始缺陷根因：`add` 只校验了空内容，未校验换行符——多行内容穿透到渲染层破坏逐行格式；修复在入口拒绝（exit_code=2 走既有错误通道），而非渲染层截断美化 | 中 | 已关闭 |

## 5. 各轮次趋势

| 轮次 | 执行用例数 | 通过 | 失败 | 新增缺陷 | 关闭缺陷 |
|------|-----------|------|------|----------|----------|
| 1 | 4 | 4 | 0 | 0 | 1（原始缺陷随变更关闭） |

一轮收敛，熔断计数 0/5。

## 6. 遗留问题与说明

- 多行历史数据不迁移（spec"明确不做"），渲染保持原样——`list` 不崩溃、退出码 0 即满足 A-4。

## 7. 产出物自检

- [x] 冻结用例全部执行，无静默跳过
- [x] 构建/Lint/静态检查结果行已填（Lint 注明未执行原因）
- [x] NFR：本变更不涉及新 NFR（沿用主运行 NFR-3 的零依赖验证，47 测试即实证）
- [x] 缺陷回填了根因/修复策略
- [x] 报告结论与用例执行结果一致
